name: Test

on:
  push:

jobs:

  test:
    name: Test
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        LETTER: [ "a", "b" ]
        QUEUE: [ "a1 b2 a3 b4" ]
    steps:

    - name: Test
      run: |
        echo "Test: ${LETTER}, ${QUEUE}."
